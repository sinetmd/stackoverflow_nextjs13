// import { NextResponse } from "next/server";

// export const PORT = async (request: Request) => {
//   const { question } = await request.json();

//   try {
//   } catch (error) {
//     return NextResponse.json({ error: error.message });
//   }
// };
